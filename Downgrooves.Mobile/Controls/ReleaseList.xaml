<?xml version="1.0" encoding="UTF-8"?>
<CollectionView x:Class="Downgrooves.Mobile.Controls.ReleaseList"
                xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
                xmlns:viewModels="clr-namespace:Downgrooves.Mobile.ViewModels">
    <CollectionView.ItemTemplate>
        <DataTemplate x:DataType="viewModels:ReleaseViewModel">
            <StackLayout Padding="10">
                <Frame BackgroundColor="{DynamicResource ButtonBackgroundColor}" Margin="1" Padding="3" HeightRequest="140" WidthRequest="140">
                    <ff:CachedImage Source="{Binding ArtworkUrl100}" />
                </Frame>
                <Label Text="{Binding CollectionName}" Style="{DynamicResource h2}" />
                <BoxView Style="{DynamicResource Separator}" />
            </StackLayout>
        </DataTemplate>
    </CollectionView.ItemTemplate>
</CollectionView>